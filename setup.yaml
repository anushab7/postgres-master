users:
  - name: springbootweb
    password: $POSTGRES_SPRINGBOOTWEB_PASSWORD
    role: superuser
databases:
  - name: springbootweb
    owner: springbootweb
    tablespace:
      - name: tb1
        location: /appz/data/tb1
    schemas:
      - name: schema1
        authorised_user: springbootweb
        search_path: true
restore:
- database: springbootweb
  source:
    url: 'https://www.ecloudcontrol.com/wp-content/uploads/2021/03/accountsdb.zip'
    user: springbootweb
    password: $POSTGRES_SPRINGBOOTWEB_PASSWORD  # password of the 'user' above.
    token: 20210415-1403  # The restore token to be validated before restoring the database. (Same as TEARDOWN_TOKEN)
